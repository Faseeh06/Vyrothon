# Copy to `.env.local` for local dev (never commit real keys).
# Groq: https://console.groq.com/keys — used only by `POST /api/groq-pipeline` (server-side).

GROQ_API_KEY=
# Optional. Fast defaults: llama-3.1-8b-instant | llama-3.3-70b-versatile
GROQ_MODEL=llama-3.1-8b-instant
